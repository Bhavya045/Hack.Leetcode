class Solution {
     class Card{
            int card;
            int count;
            public Card(int card, int count){
                this.card = card;
                this.count= count;
            }
        }
        
    public boolean isNStraightHand(int[] hand, int groupSize) {
       if(isNStraightHand%groupSize !0)return false
    }
}